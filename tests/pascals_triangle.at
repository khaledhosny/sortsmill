m4_defun([check_pascals_triangle],
[
AT_SETUP([$1])
AT_KEYWORDS([auxiliary pascals_triangle.h pascals_triangle bernstein unit spline])
AT_SKIP_IF([test x${DOING_INSTALLCHECK} = xyes])
AT_CHECK_UNQUOTED(["${abs_builddir}"/$1],[$2],[$3],[$4])
AT_CLEANUP
])

check_pascals_triangle([test_pascals_triangle],[0],[],[])

